import 'package:flutter/material.dart';

import 'theme_provider.dart';

class AppTheme {
  static ThemeData lightTheme(ThemeProvider themeProvider) => ThemeData();

  static ThemeData darkTheme(ThemeProvider themeProvider) => ThemeData();
}
